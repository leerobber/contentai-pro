# ContentAI Pro — Development Dependencies
# Extends requirements.txt (install both: pip install -r requirements.txt -r requirements-dev.txt)
-r requirements.txt

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0  # also used for async test client

# Code quality
black>=24.10.0
ruff>=0.8.0
